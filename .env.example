# Canopy Environment Variables
# Copy this file to .env and fill in your values

# ============ Anthropic API ============
# Required for Claude AI features
# Get your key from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-your-key-here

# ============ Turso Database (Vercel only) ============
# Required for web deployment on Vercel
# Create a database at: https://turso.tech
TURSO_DATABASE_URL=libsql://your-database.turso.io
TURSO_AUTH_TOKEN=your-auth-token

# ============ Google OAuth (Vercel only) ============
# Required for authentication on Vercel
# Create credentials at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# ============ Auth.js Secret (Vercel only) ============
# Generate with: openssl rand -base64 32
AUTH_SECRET=your-auth-secret-at-least-32-chars

# ============ Invite List (Vercel only) ============
# Comma-separated list of allowed email addresses
# Leave empty to allow all authenticated users (dev mode)
ALLOWED_EMAILS=user1@example.com,user2@example.com
